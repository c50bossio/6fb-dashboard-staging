'use client'\n\nimport { useState, useEffect, useMemo, useCallback } from 'react'\nimport { TrophyIcon, FireIcon, CurrencyDollarIcon, ChartBarIcon, UsersIcon } from '@heroicons/react/24/outline'\nimport { createClient } from '@/lib/supabase/client'\nimport LoadingSpinner from '../LoadingSpinner'\n\nexport default function LeaderboardComponent({ customerId, barbershopId }) {\n  const [leaderboards, setLeaderboards] = useState({})\n  const [activeLeaderboard, setActiveLeaderboard] = useState('points')\n  const [timePeriod, setTimePeriod] = useState('all_time')\n  const [loading, setLoading] = useState(true)\n  const [error, setError] = useState(null)\n  // Memoize supabase client to prevent re-creation on every render
  const supabase = useMemo(() => createClient(), [])\n\n  const leaderboardTypes = [\n    { id: 'points', name: 'Points', icon: TrophyIcon, color: 'text-yellow-600' },\n    { id: 'visits', name: 'Visits', icon: UsersIcon, color: 'text-blue-600' },\n    { id: 'spending', name: 'Spending', icon: CurrencyDollarIcon, color: 'text-green-600' },\n    { id: 'streaks', name: 'Streaks', icon: FireIcon, color: 'text-red-600' },\n    { id: 'engagement', name: 'Engagement', icon: ChartBarIcon, color: 'text-purple-600' }\n  ]\n\n  const timePeriods = [\n    { id: 'all_time', name: 'All Time' },\n    { id: 'yearly', name: 'This Year' },\n    { id: 'monthly', name: 'This Month' },\n    { id: 'weekly', name: 'This Week' }\n  ]\n\n  useEffect(() => {\n    loadLeaderboard(activeLeaderboard, timePeriod)\n  }, [activeLeaderboard, timePeriod, customerId, barbershopId])\n\n  const loadLeaderboard = async (type, period) => {\n    try {\n      setLoading(true)\n      setError(null)\n\n      // Get user session for authentication\n      const { data: { session } } = await supabase.auth.getSession()\n      if (!session) {\n        setError('Authentication required')\n        return\n      }\n\n      // Fetch leaderboard data\n      const response = await fetch(\n        `/api/customers/loyalty/gamification?action=leaderboard&type=${type}&period=${period}&limit=20`,\n        {\n          headers: {\n            'Authorization': `Bearer ${session.access_token}`\n          }\n        }\n      )\n\n      if (!response.ok) {\n        throw new Error('Failed to load leaderboard')\n      }\n\n      const data = await response.json()\n      setLeaderboards(prev => ({\n        ...prev,\n        [`${type}_${period}`]: data.leaderboard\n      }))\n      \n    } catch (err) {\n      console.error('Error loading leaderboard:', err)\n      setError(err.message)\n    } finally {\n      setLoading(false)\n    }\n  }\n\n  const currentLeaderboard = leaderboards[`${activeLeaderboard}_${timePeriod}`] || []\n  const currentUser = currentLeaderboard.find(entry => entry.customer_id === customerId)\n  const currentPosition = currentUser?.position || 'Not Ranked'\n\n  const getPositionColor = (position) => {\n    if (position === 1) return 'text-yellow-500'\n    if (position === 2) return 'text-gray-400'\n    if (position === 3) return 'text-amber-600'\n    return 'text-gray-600'\n  }\n\n  const getPositionBadge = (position) => {\n    if (position === 1) return '🥇'\n    if (position === 2) return '🥈'\n    if (position === 3) return '🥉'\n    if (position <= 10) return '⭐'\n    return ''\n  }\n\n  const formatMetricValue = (entry) => {\n    switch (activeLeaderboard) {\n      case 'points':\n        return `${entry.points?.toLocaleString()} pts`\n      case 'visits':\n        return `${entry.visits} visits`\n      case 'spending':\n        return `$${entry.total_spent?.toFixed(2)}`\n      case 'streaks':\n        return `${entry.current_streak} months`\n      case 'engagement':\n        return `${entry.engagement_score}/100`\n      default:\n        return '0'\n    }\n  }\n\n  if (loading && currentLeaderboard.length === 0) {\n    return (\n      <div className=\"flex items-center justify-center py-8\">\n        <LoadingSpinner size=\"lg\" />\n      </div>\n    )\n  }\n\n  if (error) {\n    return (\n      <div className=\"text-center py-8\">\n        <div className=\"text-red-500 mb-4\">\n          <TrophyIcon className=\"h-12 w-12 mx-auto\" />\n        </div>\n        <h3 className=\"text-lg font-medium text-gray-900 mb-2\">Unable to Load Leaderboard</h3>\n        <p className=\"text-gray-500 mb-4\">{error}</p>\n        <button\n          onClick={() => loadLeaderboard(activeLeaderboard, timePeriod)}\n          className=\"px-4 py-2 bg-brand-600 text-white rounded-lg hover:bg-brand-700 transition-colors\"\n        >\n          Try Again\n        </button>\n      </div>\n    )\n  }\n\n  return (\n    <div className=\"space-y-6\">\n      {/* Current User Position */}\n      {currentUser && (\n        <div className=\"bg-gradient-to-r from-brand-50 to-blue-50 border border-brand-200 rounded-lg p-4\">\n          <div className=\"flex items-center justify-between\">\n            <div className=\"flex items-center\">\n              <div className=\"text-2xl mr-3\">{getPositionBadge(currentUser.position)}</div>\n              <div>\n                <h3 className=\"font-medium text-gray-900\">Your Position</h3>\n                <p className=\"text-sm text-gray-500\">You're ranked #{currentUser.position} this {timePeriod.replace('_', ' ')}</p>\n              </div>\n            </div>\n            <div className=\"text-right\">\n              <div className=\"text-lg font-bold text-brand-600\">{formatMetricValue(currentUser)}</div>\n              <div className=\"text-sm text-gray-500\">{currentUser.achievements_count} achievements</div>\n            </div>\n          </div>\n        </div>\n      )}\n\n      {/* Controls */}\n      <div className=\"flex flex-col sm:flex-row gap-4\">\n        {/* Leaderboard Type Selector */}\n        <div className=\"flex-1\">\n          <label className=\"block text-sm font-medium text-gray-700 mb-2\">Leaderboard Type</label>\n          <div className=\"grid grid-cols-3 sm:grid-cols-5 gap-2\">\n            {leaderboardTypes.map((type) => {\n              const Icon = type.icon\n              return (\n                <button\n                  key={type.id}\n                  onClick={() => setActiveLeaderboard(type.id)}\n                  className={`flex flex-col items-center p-3 rounded-lg border-2 transition-all ${\n                    activeLeaderboard === type.id\n                      ? 'border-brand-500 bg-brand-50'\n                      : 'border-gray-200 hover:border-gray-300'\n                  }`}\n                >\n                  <Icon className={`h-5 w-5 mb-1 ${activeLeaderboard === type.id ? 'text-brand-600' : type.color}`} />\n                  <span className={`text-xs font-medium ${\n                    activeLeaderboard === type.id ? 'text-brand-600' : 'text-gray-700'\n                  }`}>\n                    {type.name}\n                  </span>\n                </button>\n              )\n            })}\n          </div>\n        </div>\n\n        {/* Time Period Selector */}\n        <div>\n          <label className=\"block text-sm font-medium text-gray-700 mb-2\">Time Period</label>\n          <select\n            value={timePeriod}\n            onChange={(e) => setTimePeriod(e.target.value)}\n            className=\"px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-500 focus:border-brand-500\"\n          >\n            {timePeriods.map((period) => (\n              <option key={period.id} value={period.id}>{period.name}</option>\n            ))}\n          </select>\n        </div>\n      </div>\n\n      {/* Leaderboard Table */}\n      <div className=\"bg-white border border-gray-200 rounded-lg overflow-hidden\">\n        <div className=\"px-4 py-3 bg-gray-50 border-b border-gray-200\">\n          <h3 className=\"text-lg font-medium text-gray-900\">\n            {leaderboardTypes.find(t => t.id === activeLeaderboard)?.name} Leaderboard\n          </h3>\n          <p className=\"text-sm text-gray-500\">\n            {timePeriods.find(p => p.id === timePeriod)?.name}\n          </p>\n        </div>\n\n        {currentLeaderboard.length === 0 ? (\n          <div className=\"text-center py-8\">\n            <TrophyIcon className=\"h-12 w-12 mx-auto text-gray-400 mb-4\" />\n            <h3 className=\"text-lg font-medium text-gray-900 mb-2\">No Data Available</h3>\n            <p className=\"text-gray-500\">No leaderboard data for this period yet.</p>\n          </div>\n        ) : (\n          <div className=\"divide-y divide-gray-200\">\n            {currentLeaderboard.map((entry, index) => {\n              const isCurrentUser = entry.customer_id === customerId\n              return (\n                <div\n                  key={entry.customer_id}\n                  className={`flex items-center justify-between p-4 ${\n                    isCurrentUser ? 'bg-brand-50' : 'hover:bg-gray-50'\n                  }`}\n                >\n                  <div className=\"flex items-center\">\n                    <div className={`text-xl font-bold w-12 text-center ${\n                      getPositionColor(entry.position)\n                    }`}>\n                      {entry.position <= 3 ? getPositionBadge(entry.position) : `#${entry.position}`}\n                    </div>\n                    \n                    <div className=\"ml-4\">\n                      <div className={`font-medium ${\n                        isCurrentUser ? 'text-brand-900' : 'text-gray-900'\n                      }`}>\n                        {entry.customer_name}\n                        {isCurrentUser && (\n                          <span className=\"ml-2 px-2 py-1 bg-brand-100 text-brand-800 text-xs rounded-full\">\n                            You\n                          </span>\n                        )}\n                      </div>\n                      <div className=\"text-sm text-gray-500 flex items-center\">\n                        <span className=\"mr-3\">{entry.achievements_count} achievements</span>\n                        {entry.current_streak > 0 && (\n                          <span className=\"flex items-center\">\n                            <FireIcon className=\"h-3 w-3 text-red-500 mr-1\" />\n                            {entry.current_streak} streak\n                          </span>\n                        )}\n                        {entry.tier && (\n                          <span className=\"ml-3 px-2 py-1 bg-gray-100 text-gray-600 text-xs rounded\">\n                            {entry.tier}\n                          </span>\n                        )}\n                      </div>\n                    </div>\n                  </div>\n                  \n                  <div className=\"text-right\">\n                    <div className={`text-lg font-bold ${\n                      isCurrentUser ? 'text-brand-600' : 'text-gray-900'\n                    }`}>\n                      {formatMetricValue(entry)}\n                    </div>\n                    {entry.member_since && (\n                      <div className=\"text-xs text-gray-500\">\n                        Member since {new Date(entry.member_since).toLocaleDateString()}\n                      </div>\n                    )}\n                  </div>\n                </div>\n              )\n            })}\n          </div>\n        )}\n      </div>\n\n      {loading && (\n        <div className=\"flex items-center justify-center py-4\">\n          <LoadingSpinner size=\"sm\" />\n          <span className=\"ml-2 text-sm text-gray-500\">Updating leaderboard...</span>\n        </div>\n      )}\n    </div>\n  )\n}"