[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "nodejs-18_x"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python3 railway-start.py"